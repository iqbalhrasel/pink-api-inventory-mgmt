spring:
  application:
    name: pink_api
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 1800 #20m
    refreshTokenExpiration: 39600 #11 hours